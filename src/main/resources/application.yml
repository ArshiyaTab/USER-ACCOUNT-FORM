server:
  port: 9080
Spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password: sa
    driver-class-name: org.h2.Driver
 
  jpa:
   properties:
      hibernate:
   show_sql: true
   defer-datasource-initialization: true
  mail:
       host: smtp.gmail.com
       port: 587
       username: devops.arsalan1@gmail.com
       password: null
       Properties:
         mail:
            smtp:
             startls:
               enable: true
            auth: true